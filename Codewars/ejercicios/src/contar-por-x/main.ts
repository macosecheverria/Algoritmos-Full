const countBy = (firtsIndex: number, lastIndex: number): number[] => {
    let totalArray: number[] = [];  
    let previusValue = 0; 

    for(let i = 0; i <= lastIndex; i++){
        firtsIndex += firtsIndex
        console.log(previusValue += firtsIndex);
    }

   
    return totalArray;
}

console.log(countBy(1,10));